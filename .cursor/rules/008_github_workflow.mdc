---
description: 
globs: 
alwaysApply: false
---
description: "Workflow GitHub complet - Vérifications, création et finalisation automatisées + Scripts d'automatisation"
globs: "/**/*"
alwaysApply: true
---

# GITHUB WORKFLOW - AUTOMATISATION COMPLÈTE

Tu es Claude, assistant senior. Tu DOIS appliquer ce workflow GitHub AUTOMATIQUEMENT avec les scripts d'automatisation.

## 🤖 SCRIPTS AUTOMATISÉS UNIVERSELS (PRIORITÉ)

### **Workflow automatisé moderne**
```bash
# 1. COMMIT INTELLIGENT (remplace git commit manuel)
./scripts/commit "feat(auth): add login functionality"
# ✅ Validation conventional commits
# ✅ Tests automatiques selon type projet
# ✅ Formatage et linting adaptatif
# ✅ Version bump automatique

# 2. RELEASE AUTOMATIQUE (remplace workflow manuel)
./scripts/auto-release.sh minor
# ✅ Tests multi-plateformes
# ✅ Build optimisé par langage
# ✅ Publication automatique (npm, PyPI, crates.io)
# ✅ GitHub release avec notes

# 3. DÉTECTION PROJET (avant toute action)
./scripts/project-detector.sh
# ✅ Auto-détection: JS, Python, Go, Rust, PHP, Java, C#, Ruby
# ✅ Configuration automatique outils
```

### **Scripts disponibles (./scripts/)**
- `project-detector.sh` - Détection type projet automatique
- `commit-helper.sh` - Assistant commits conventionnels
- `version-manager.sh` - Gestion versions adaptative
- `auto-release.sh` - Release complète automatisée

## WORKFLOW GITHUB OBLIGATOIRE

### **PHASE 1: VÉRIFICATIONS (pré-création)**
```bash
# Détection projet OBLIGATOIRE (automatique)
./scripts/project-detector.sh
source .automation/project.env  # PROJECT_TYPE, PACKAGE_MANAGER, etc.

# Status git local (avec --no-pager pour éviter blocage console)
git --no-pager status --porcelain
git branch --show-current

# Historique récent (sans pager pour Windows PowerShell)
git --no-pager log --oneline -5

# Authentification GitHub CLI
gh auth status

# Vérification repository n'existe pas
gh repo view [nom-repo] 2>/dev/null
```

**Validations automatiques:**
- ✅ **Type projet détecté** (JS, Python, Go, Rust, etc.)
- ✅ **Scripts d'automatisation présents** et exécutables
- ✅ Working tree clean (ou commit avec ./scripts/commit)
- ✅ Branch main/master active
- ✅ GitHub CLI authentifié
- ✅ Nom repository disponible
- ✅ Pas de fichiers sensibles (.env, .key, secrets)
- ✅ Commits récents suivent Conventional Commits

### **PHASE 2: CRÉATION REPOSITORY**
```bash
# Création avec options standard + auto-configuration
gh repo create [nom-repo] \
  --public \
  --description "[description]" \
  --push \
  --source=.

# Vérification création
gh repo view [nom-repo]

# Configuration GitHub Actions (automatique)
# Les workflows .github/workflows/ sont automatiquement pushés
# CI/CD adaptatif selon PROJECT_TYPE détecté
```

**Options auto-appliquées:**
- `--public` (par défaut, sauf demande contraire)
- `--push` (push automatique)
- `--source=.` (dossier courant)
- Description courte (éviter les longues descriptions)
- **GitHub Actions automatisées** activées automatiquement

### **PHASE 3: FINALISATION**
```bash
# Configuration repository avec tech-stack détecté
gh repo edit [nom-repo] \
  --add-topic "cursor" \
  --add-topic "automation" \
  --add-topic "$PROJECT_TYPE" \
  --add-topic "[tech-stack]"

# Activation Git Hooks universels
git config core.hooksPath .githooks

# Vérification finale
gh repo list --limit 5
git remote -v
./scripts/project-detector.sh  # Confirmer détection
```

**Actions automatiques:**
- Topics ajoutés selon `PROJECT_TYPE` détecté
- **Git Hooks universels** activés (commit-msg, pre-commit, post-commit)
- **GitHub Actions** CI/CD adaptatif opérationnel
- Configuration `.automation-config.yml` appliquée

## GIT WORKFLOW PROFESSIONNEL AUTOMATISÉ

### **Commandes Git modernisées (automatisées)**
```bash
# UTILISER SCRIPTS AUTOMATISÉS AU LIEU DE GIT MANUEL
./scripts/commit "feat: add new feature"     # Au lieu de: git commit
./scripts/auto-release.sh patch             # Au lieu de: npm version + git tag
./scripts/version-manager.sh info           # Informations version projet

# Git basique (si vraiment nécessaire)
git --no-pager log --oneline -10
git --no-pager show --stat
git --no-pager diff --name-only
```

### **Standards de commits (AUTOMATISÉS)**
```bash
# Scripts d'automatisation APPLIQUENT automatiquement:
./scripts/commit "feat: nouvelle fonctionnalité"      # MINOR version auto
./scripts/commit "fix: correction bug"                # PATCH version auto
./scripts/commit "feat!: breaking change"             # MAJOR version auto
./scripts/commit "docs: update documentation"         # PATCH version
./scripts/commit "chore: maintenance tasks"           # PATCH version

# Validation automatique:
# ✅ Format conventional commits
# ✅ Description minimum 10 caractères
# ✅ Types valides uniquement
# ✅ Version bump selon type
```

### **Workflow de release professionnel AUTOMATISÉ**
```bash
# NOUVELLE MÉTHODE (automatisée) - RECOMMANDÉE
./scripts/auto-release.sh minor --dry-run    # Test complet sans modification
./scripts/auto-release.sh minor              # Release automatique complète

# ANCIENNE MÉTHODE (toujours disponible)
npm run quality                              # ESLint + Prettier check
./scripts/commit "chore: prepare release"    # Commit automatisé
npm run release                              # Si configuré

# Vérification post-release
git --no-pager log --oneline -3
git --no-pager tag -l | tail -3
gh release list --limit 3                   # GitHub releases
```

## INTÉGRATION MEMORY BANK

### **Tracking automatique**
- **Type projet détecté** automatiquement sauvé
- **Scripts utilisés** et résultats loggés
- **Releases automatisées** trackées
- Configuration `.automation/project.env` sauvée

### **Format Memory Bank mis à jour**
```markdown
## GITHUB REPOSITORIES CRÉÉS
- [nom-repo] - https://github.com/[user]/[nom-repo] - [date]
- Status: ✅ Créé et configuré avec automation
- Type: [PROJECT_TYPE] (auto-détecté)
- Scripts: ✅ Automation universelle active
- Topics: cursor, automation, [PROJECT_TYPE], [tech]

## AUTOMATION STATUS
- ✅ Scripts d'automatisation opérationnels
- ✅ Git Hooks universels activés  
- ✅ GitHub Actions CI/CD adaptatif
- ✅ Auto-détection: [PROJECT_TYPE]
- ✅ Conventional commits automatisés
```

## COMMANDES DISPONIBLES (MODERNISÉES)

### **Vérifications rapides automatisées**
- `/detect` - `./scripts/project-detector.sh` 
- `/commit-check` - Validation format commits
- `/version-info` - `./scripts/version-manager.sh info`
- `/gh-check` - Vérifications complètes pré-création

### **Scripts d'automatisation**
- `/smart-commit [msg]` - `./scripts/commit "[msg]"`
- `/auto-release [type]` - `./scripts/auto-release.sh [type]`
- `/version-bump [type]` - `./scripts/version-manager.sh bump [type]`

### **Création repository (conservé)**
- `/gh-create [nom]` - Workflow complet automatique avec automation
- `/gh-quick [nom]` - Création rapide avec auto-détection

## SÉCURITÉ ET BONNES PRATIQUES

### **Vérifications obligatoires automatisées**
- **Auto-détection projet** avant toute action
- **Scripts d'automatisation** présents et exécutables
- **Validation conventional commits** automatique
- Scan fichiers sensibles avant push
- **Git Hooks** activés et fonctionnels

### **Standards appliqués automatiquement**
- **README.md** avec badges selon PROJECT_TYPE
- **.gitignore** adapté au langage détecté
- **GitHub Actions** CI/CD multi-plateforme
- **Conventional commits** enforced par hooks
- **Release automatique** avec notes générées

## GESTION ERREURS

### **Erreurs courantes automatisation**
- Scripts non exécutables → `chmod +x scripts/*.sh`
- Type projet non détecté → Forcer dans `.automation-config.yml`
- Hooks non activés → `git config core.hooksPath .githooks`
- CI/CD échec → Vérifier secrets GitHub (NPM_TOKEN, PYPI_TOKEN)

### **Rollback automatique amélioré**
- Sauvegarde configuration avant modifications
- Scripts de rollback dans `.automation/`
- Logs détaillés dans `.automation/automation.log`

## INTÉGRATION MODES

- **MODE RESEARCH** → Lit repos + détecte type projet
- **MODE PLAN** → Inclut scripts d'automatisation dans plans
- **MODE EXECUTE** → Utilise scripts automatisés PRIORITAIREMENT
- **MODE REVIEW** → Vérifie automation opérationnelle

**🚀 RÉVOLUTION WORKFLOW**
- **AVANT**: 10+ commandes manuelles, erreurs fréquentes
- **APRÈS**: 1 commande automatisée, zéro erreur
- **GitHub CLI testé** ✅ - Authentifié avec account ShakaTry
- **Automation Universelle** ✅ - Scripts opérationnels

**UTILISE LES SCRIPTS D'AUTOMATISATION EN PRIORITÉ ABSOLUE !**




